---
phase: 04-real-time-operations-and-analytics
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - apps/web/src/app/(menu)/menu/[slug]/order/[orderId]/page.tsx
  - apps/web/src/components/ordering/order-status.tsx
autonomous: false
requirements: [MGMT-04]

must_haves:
  truths:
    - "Customer order status page updates in real-time via WebSocket push (not polling)"
    - "If WebSocket fails after 3 attempts, customer page falls back to 5-second polling silently"
    - "Status transitions pushed by venue owner appear on customer page within 1 second"
  artifacts:
    - path: "apps/web/src/components/ordering/order-status.tsx"
      provides: "WebSocket-upgraded order status component"
      contains: "socket"
    - path: "apps/web/src/app/(menu)/menu/[slug]/order/[orderId]/page.tsx"
      provides: "Server Component passing initial data to WS-enabled client"
      contains: "OrderStatus"
  key_links:
    - from: "apps/web/src/components/ordering/order-status.tsx"
      to: "apps/web/src/lib/socket.ts"
      via: "import socket singleton"
      pattern: "import.*socket.*from.*@/lib/socket"
    - from: "apps/web/src/components/ordering/order-status.tsx"
      to: "order:updated WebSocket event"
      via: "socket.on('order:updated')"
      pattern: "order:updated"
---

<objective>
Upgrade the customer order status page from 5-second polling to WebSocket push, and perform final human verification of the complete Phase 4 feature set.

Purpose: Completes the real-time loop — venue owner status changes propagate instantly to the customer's tracking page. The checkpoint verifies the entire Phase 4 works end-to-end.

Output: WebSocket-upgraded customer order status page with polling fallback, plus human verification of all Phase 4 features.
</objective>

<execution_context>
@/home/alfie/.claude/get-shit-done/workflows/execute-plan.md
@/home/alfie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-operations-and-analytics/04-CONTEXT.md
@.planning/phases/04-real-time-operations-and-analytics/04-RESEARCH.md
@.planning/phases/04-real-time-operations-and-analytics/04-01-SUMMARY.md
@.planning/phases/04-real-time-operations-and-analytics/04-03-SUMMARY.md

@apps/web/src/app/(menu)/menu/[slug]/order/[orderId]/page.tsx
@apps/web/src/components/ordering/order-status.tsx
@apps/web/src/lib/socket.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade customer order status from polling to WebSocket push with fallback</name>
  <files>
    apps/web/src/components/ordering/order-status.tsx
    apps/web/src/app/(menu)/menu/[slug]/order/[orderId]/page.tsx
  </files>
  <action>
    1. Modify `apps/web/src/components/ordering/order-status.tsx`:
       - Import `socket` from `@/lib/socket`.
       - Replace the existing `usePolling` hook with a WebSocket-based approach:
         - In a `useEffect` keyed on `[orderId]`:
           - Connect WITHOUT auth token: `socket.auth = {}` (anonymous customer connection — two-tier auth in gateway allows no-token connections).
           - Call `socket.connect()`.
           - On `connect`: emit `'join:order'` with the `orderId`.
           - On `'order:updated'` event: if `update.id === orderId`, update the status state with `update.status`. Also call `router.refresh()` to re-fetch server component data for any other fields that might have changed.
           - On `connect_error`: if `!socket.active` (3 attempts exhausted), start a 5-second polling interval using `router.refresh()` as the existing polling mechanism. This is the silent fallback per user decision.
         - Cleanup: `socket.off()` all named listeners, `socket.disconnect()`, clear polling interval.

       - Keep the existing progress steps UI (RECEIVED -> PREPARING -> READY), terminal state detection, and status index mapping. Only the data fetching mechanism changes from polling to WebSocket.

       - Remove the `usePolling` hook usage and implement the fallback polling inline: a `setInterval(() => router.refresh(), 5000)` activated only when WebSocket fails.

       - IMPORTANT: The customer page currently uses `usePolling` which calls `router.refresh()` on an interval. The WebSocket upgrade replaces this: status updates come via WS event, and on terminal states (COMPLETED, CANCELLED, READY) stop listening (disconnect socket). If WS fails, the fallback interval does the same `router.refresh()`.

    2. Modify `apps/web/src/app/(menu)/menu/[slug]/order/[orderId]/page.tsx` (if needed):
       - The Server Component currently fetches order data and passes it to OrderStatus. This pattern remains. No changes needed here unless OrderStatus props need updating.
       - Verify the page still works with cache: 'no-store' so router.refresh() gets fresh data.

    AVOID: Sending an auth token from the customer page (customers have no JWT). Do NOT remove the terminal state check — the socket should disconnect when the order reaches COMPLETED, CANCELLED, or READY (no more updates expected). Do NOT forget to handle the case where the socket connects but the order is already in a terminal state (check on mount and skip WebSocket setup if already terminal).
  </action>
  <verify>
    <automated>cd /home/alfie/bite-byte && pnpm --filter web exec tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify order-status.tsx imports socket singleton, connects without auth, emits join:order, listens for order:updated, has polling fallback on WS failure, disconnects on terminal state.</manual>
  </verify>
  <done>Customer order status page uses WebSocket for real-time updates. Connects anonymously, joins order room, receives status pushes. Falls back to 5-second polling if WebSocket fails after 3 attempts. Disconnects on terminal states. Zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 4 feature set</name>
  <files>none</files>
  <action>
    Human verification of all Phase 4 features. No automated implementation — this is a verification checkpoint.

    Prerequisites: Both apps running (`pnpm dev` from monorepo root). At least one venue with menu items exists.

    **1. Live Orders Dashboard (MGMT-01, MGMT-02, MGMT-03)**
    1. Navigate to /venues/[venueId]/orders in the dashboard
    2. In another browser tab, place a PAY_AT_COUNTER order from the customer menu
    3. Verify: The new order appears on the kanban board within 1 second
    4. Verify: An audio alert plays (if not muted)
    5. Verify: The order card shows reference code, customer name, item count, time ago, total
    6. Click on the order card to expand it
    7. Verify: Full item list and action buttons are visible
    8. Click "Start Preparing" — verify order moves to Preparing column
    9. Click "Mark Ready" — verify order moves to Ready column
    10. Click "Complete" — verify order auto-hides from board after ~30 seconds
    11. Test mute/unmute toggle

    **2. Customer Real-Time Updates (MGMT-04)**
    12. Place a new order and navigate to the order status page
    13. From the dashboard, transition the order through statuses
    14. Verify: Customer status page updates within 1 second (no manual refresh)

    **3. Connection Resilience**
    15. Stop the NestJS API server briefly
    16. Verify: Yellow "Connection lost" banner appears on dashboard
    17. Restart the API server
    18. Verify: Banner disappears, orders re-fetch automatically

    **4. Analytics (ANLY-01, ANLY-02, ANLY-03)**
    19. Navigate to /venues/[venueId]/analytics
    20. Verify: Revenue cards show today/week/month totals
    21. Verify: Bar chart shows order volume per day
    22. Verify: Top 5 selling items list displays

    **5. Order History (MGMT-05)**
    23. Navigate to /venues/[venueId]/history
    24. Verify: Completed and cancelled orders appear in table
    25. Test date filtering — apply a date range and verify results change
    26. Test pagination if enough orders exist

    **6. Sidebar Navigation**
    27. Verify: Orders, History, Analytics links appear for each venue in the sidebar
    28. Verify: Active state highlights correctly when on each page
  </action>
  <verify>
    <automated>cd /home/alfie/bite-byte && pnpm --filter web exec tsc --noEmit && cd /home/alfie/bite-byte/apps/api && npx tsc --noEmit</automated>
    <manual>Human verifies all 28 checks listed in action section above</manual>
  </verify>
  <done>All Phase 4 features verified by human: live orders dashboard with real-time WebSocket, audio alerts, status management, customer status page with WS push, analytics dashboard, and order history page. Type "approved" to complete Phase 4 or describe issues found.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation clean across both apps
2. WebSocket connects from customer page without auth
3. Real-time status updates propagate owner -> customer within 1 second
4. All analytics endpoints return data
5. Order history filters and paginates correctly
6. Sidebar navigation is complete and functional
</verification>

<success_criteria>
- Customer order status page uses WebSocket for instant updates
- Polling fallback works when WebSocket is unavailable
- All Phase 4 success criteria from ROADMAP.md verified by human:
  1. New orders appear on dashboard within 1 second with audio alert
  2. Status transitions propagate to customer in real-time
  3. Reconnect re-fetches via REST before resuming WS feed
  4. Order history filterable by date
  5. Revenue totals, top items, and order volume chart display correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-operations-and-analytics/04-05-SUMMARY.md`
</output>
